---
title: "Getting started"
description: "Build your first UI in seconds with UI Lab components."
category: "guides"
publishedOn: "2025-01-15"
tags: ["getting-started", "setup", "basics"]
---

# Getting started

Build your first UI in seconds with UI Lab components.

## Who this is for

**Prerequisites:**
- Node.js 18 or higher
- React 19 or higher
- Tailwind CSS v4 configured
- Basic React component knowledge

**You will learn:**
- How to import and use UI Lab components
- Common component patterns for layouts and forms
- How to customize component behavior and styling

**Estimated time:** 10 minutes

## Installation

Start with our [Installation guide](/docs/installation) to add UI Lab to your project.

## Your first component

Drop a button anywhere—it just works:

```tsx
import { Button } from "ui-lab-components";

export default function Home() {
  return (
    <div className="flex items-center gap-4">
      <Button>Default</Button>
      <Button variant="secondary">Secondary</Button>
      <Button size="lg">Large</Button>
      <Button loading>Loading...</Button>
    </div>
  );
}
```

## Common patterns

### Login form

```tsx
import { Button, Card, Input, Label } from "ui-lab-components";

export function LoginCard() {
  return (
    <Card className="w-full max-w-sm">
      <Card.Header>
        <Card.Title>Welcome back</Card.Title>
      </Card.Header>
      <Card.Content className="space-y-4">
        <div>
          <Label htmlFor="email">Email</Label>
          <Input id="email" type="email" placeholder="you@example.com" />
        </div>
        <div>
          <Label htmlFor="pass">Password</Label>
          <Input id="pass" type="password" />
        </div>
      </Card.Content>
      <Card.Footer>
        <Button className="w-full">Sign in</Button>
      </Card.Footer>
    </Card>
  );
}
```

### Dashboard widget

```tsx
import { Card, Badge } from "ui-lab-components";

export function RevenueWidget() {
  return (
    <Card className="p-6">
      <div className="flex items-center justify-between">
        <div>
          <p className="text-foreground-400 text-sm">Revenue</p>
          <p className="text-2xl font-semibold">$45,231</p>
        </div>
        <Badge>+12.5%</Badge>
      </div>
    </Card>
  );
}
```

## Next steps

- [Browse components](/components) – Full gallery with all available components
- [Explore design system](/docs/design-system) – Understand UI Lab's architecture and principles
- [Learn theming](/docs/theming) – Customize colors, radius, and tokens for your brand
- [AI workflow](/docs/ai-integration) – Generate code with Cursor, Claude, or Copilot
