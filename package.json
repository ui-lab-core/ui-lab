{
  "name": "ui-lab-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "UI Lab - A modern component library",
  "packageManager": "pnpm@10.21.0",
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "dev": "pnpm --filter ./apps/* run dev",
    "dev:site": "pnpm --filter @ui-lab/site run dev",
    "build": "pnpm -r run build",
    "build:packages": "pnpm --filter './packages/*' run build",
    "build:site": "pnpm --filter @ui-lab/site run build",
    "build:push": "pnpm run build:packages",
    "type-check": "pnpm -r run type-check",
    "lint": "pnpm -r run lint",
    "clean": "pnpm -r run clean",
    "publish": "pnpm publish -r",
    "versions:status": "node scripts/toggle-versions.js status",
    "versions:to-workspace": "node scripts/toggle-versions.js to-workspace",
    "versions:to-release": "node scripts/toggle-versions.js to-version"
  },
  "dependencies": {
    "next": "^16.0.10"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
