<behavior>
Verify existing code, especially redundant or hand-rolled mechanisms. Generalize failures before patching. Prefer deletion and restored defaults over additive workarounds. Fix structural causes, not symptoms.
</behavior>
